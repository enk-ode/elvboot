env ELEBAKE_BATCH_KEEP_GOING=0 sh -c 'tmp=$(mktemp "$ELEBAKE_BASE/.tmp/batch.XXXXXX"); chmod 0600 "$tmp"; cat > "$tmp"; exec "$ELEBAKE_CONTEXT_SCRIPT" batch "$tmp" true'
# 'stage pool export' is a batch: its checks must stop it even inside a keep-going
# sync (KEEP_GOING=1 there), so the pin forces fail-fast for its own lines.
# @see ELEBAKE_INTERPRETER_stage_tree_work
