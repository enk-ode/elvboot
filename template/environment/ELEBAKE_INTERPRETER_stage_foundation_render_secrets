cat
# @summary Interpreter for `stage foundation render secrets` -- one section of foundation.c as TEXT
# `stage foundation make` redirects it into foundation.c.new. # Display terminal; cat so the text survives an executing terminal default.
# @see ELEBAKE_INTERPRETER_stage_foundation_make
