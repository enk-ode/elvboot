sh
# Act terminal 'filter full' -- writes the filtered collection; sh so it
# acts. The agnostic pin ELEBAKE_INTERPRETER_filter does NOT cover this
# name: pin lookup strips underscores and arity, so _filter_full2 resolves to
# filter_full, never to filter.
