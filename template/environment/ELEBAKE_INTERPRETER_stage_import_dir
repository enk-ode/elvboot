sh
# Terminal behind 'stage import <stage> <reldir>' (directory declaration);
# pinned to sh so a restore's replay takes effect directly. The validating
# combinators above it (___stage_import2/3) keep their batch type default --
# per the arity rule there is NO pin on the heterogeneous stage_import name.
