cat
# @summary Interpreter for the dump block `stage dump inventory set` -- dump TEXT
# Batch machinery: the emitted 'stage inventory add' replay lines are displayed,
# not executed, so they become part of the dump.
# @see ELEBAKE_INTERPRETER_stage_dump_kenv
