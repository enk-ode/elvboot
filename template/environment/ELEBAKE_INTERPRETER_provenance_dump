cat
# @summary Interpreter for `provenance dump` -- dump TEXT
# Batch machinery: the emitted import lines are displayed, not executed, so
# they become part of the dump.
# @see ELEBAKE_INTERPRETER_dump_env
