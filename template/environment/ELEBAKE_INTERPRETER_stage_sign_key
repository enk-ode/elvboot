sh
# Interpreter override for the 'stage_sign_key' terminal, pinned to 'sh'.
# `stage sign key` is DB bookkeeping (write the sign-key slot symlink) and should
# take effect, like setenv — not stay display-only under the cat default.
