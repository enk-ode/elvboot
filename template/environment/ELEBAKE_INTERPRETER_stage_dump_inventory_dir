cat
# @summary Interpreter for the dump block `stage dump inventory dir` -- dump TEXT
# Batch machinery: the emitted 'stage import' declaration is displayed, not
# executed, so it becomes part of the dump.
# @see ELEBAKE_INTERPRETER_stage_dump_inventory_set
