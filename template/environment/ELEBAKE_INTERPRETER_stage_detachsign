sh
# Act terminal 'stage attest' (detached manifest signature) -- runs as
# the CALLING user: boot/ is theirs and gpg needs their agent, pinentry
# and card. The former sudo pin CAUSED the root-gpg mismatch (tutorial
# find #18, the trust-anchor lesson repeated).
