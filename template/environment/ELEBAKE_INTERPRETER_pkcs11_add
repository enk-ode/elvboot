sh
# Interpreter override for 'pkcs11 add' (_pkcs11_add3), pinned to 'sh' so the
# registration (record dir, public files, named symlink) takes effect —
# regardless of the safe-by-default TERMINAL_INTERPRETER=cat. Inspect first by
# running it under cat if you want to see the commands before they execute.
