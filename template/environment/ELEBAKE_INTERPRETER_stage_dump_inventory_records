cat
# @summary Interpreter for the dump block `stage dump inventory records` -- dump TEXT
# Batch machinery: the emitted 'stage import' lines are displayed, not executed,
# so they become part of the dump (the records travel in the bundle).
# @see ELEBAKE_INTERPRETER_stage_dump_marker
