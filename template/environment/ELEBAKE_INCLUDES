database.sh pkcs11.sh openpgp.sh pem.sh freebsd.sh prerequisites.sh stage.sh foundation.sh intp.sh helpenv.sh help.sh inspect.sh
# @summary Modules preloaded at startup (engine.sh is always loaded first)
# With a bootstrapped DB the full list is preloaded (behaviour as before).
# Without a DB the built-in minimal default applies (database.sh help.sh --
# the no-DB commands bootstrap and help); every other module is lazy-loaded
# on dispatch via the generated FUNCTION_MODULES mapping, so trimming this
# list changes preloading only, never reachability.
