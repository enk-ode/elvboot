sudo sh
# @summary Interpreter for `stage tree close` -- rollback when the tree is not the manifest
# Decided at generation time; the emission is one zfs rollback (or a note).
# Needs root (the imported pool / the mounted medium). Inspect first by
# running the command under cat. To protect this step (display instead of
# execute), remove or override this pin.
# @see ELEBAKE_INTERPRETER_stage_tree_sync
