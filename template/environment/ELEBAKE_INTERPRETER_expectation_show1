cat
# Arity-specific ON PURPOSE: the 0-arg sibling is a batch (one re-invocation
# per record) and must keep the batch default.
# Display terminal 'expectation show' -- renders the C that WOULD be
# emitted; cat so the view survives an executing terminal default.
