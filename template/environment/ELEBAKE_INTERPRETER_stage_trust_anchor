sh
# Act terminal 'stage trust anchor' -- writes ONLY into the stage's
# worktree (brj-owned) and runs gpg: as the calling user, with their
# agent, pinentry and card. sudo here was a ported-over reflex that
# CAUSED the root-gpg/keyring mismatch (tutorial find #9).
