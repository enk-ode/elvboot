cat
# @summary Interpreter for `stage filter uncurated` -- one view of the curation
# Display terminal; cat so the text survives an executing terminal default.
# @see ELEBAKE_INTERPRETER_stage_filter_add
